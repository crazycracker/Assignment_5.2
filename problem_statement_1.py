subjects = ['Americans','Indians']
verbs = ['play', 'watch']
objects = ['Baseball', 'cricket']

for sub in subjects:
    for verb in verbs:
        for obj in objects:
            print(sub + ' ' + verb + ' ' + obj)
